Placeholder source for the optional VectorDrive image.

Set `VECTORDRIVE_EXT_PATH` to a directory containing prebuilt `lib/*.so` and
`share/extension/*` files when building `postgresql-18-cnpg-image-vd`.
