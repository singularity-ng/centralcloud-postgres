# Release is handled by Forgejo CI (.forgejo/workflows/release.yml).
# Image is published to registry.infra.centralcloud.com/centralcloud/centralcloud-postgres
# This file is kept as a placeholder so GitHub mirror PRs don't show a missing workflow.
name: Release (disabled — see Forgejo)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build and publish runs on Forgejo. See .forgejo/workflows/release.yml"
