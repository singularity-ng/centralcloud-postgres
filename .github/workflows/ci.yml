# CI runs on Forgejo (.forgejo/workflows/ci.yml). This placeholder keeps the
# GitHub mirror from showing a missing workflow.
name: CI (disabled — see Forgejo)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Checks run on Forgejo. See .forgejo/workflows/ci.yml"
